baseURL = "https://getrad.ar"
languageCode = "pt"
title = "Radar"
buildFuture = true
timeZone = "Europe/Lisbon"

[params]
  description = "Short briefings and curated channels for bespoke audiences."
  tagline = "What matters, why it matters, and the read that stays."

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true

[outputs]
  home = ["HTML"]
  section = ["HTML"]
  page = ["HTML"]
